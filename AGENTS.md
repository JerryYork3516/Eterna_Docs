# Eterna · 文档仓库协作规则 · v1

> 约束本仓库中文档的权威边界、统一格式和变更流程。
> 适用于人工维护者与所有参与 Eterna 文档工作的代理。

---

## 仓库定位

本仓库是 Eterna 正式文档与知识体系的唯一权威来源。`00_Eterna` 至 `06_研究与探索` 中经过审核的文档属于正式知识库；`99_Agents工作空间` 仅用于非正式草稿和代理协作，不构成权威定义。

---

## Markdown 格式

- 一级标题统一使用 `# 主题 · 文档类型 · 版本`；需要阶段信息时，可在版本前增加 `· Stage N`。
- 标题后使用引用块说明文档定位、适用范围或当前状态。
- 主要章节之间使用 `---` 分隔。
- `##` 用于主要章节，`###` 用于章节内子项，不跳级使用标题。
- 顺序流程使用编号列表，并列规则使用项目列表，命令、数据结构和交互流程使用带语言标识的代码块。
- 正文内容变化时递增文档版本；仅修正排版或错别字时可保持原版本。

---

## 内容规则

- Markdown 文件统一使用 UTF-8 和中文正文。
- 产品与平台名称使用官方写法，例如 `macOS`、`visionOS`、`watchOS`。
- 未经审核，不得从 Eterna Studio、Aftelle 或其他仓库直接导入正文。
- 正式文档新增、移动或变更时，必须同步更新根目录 `INDEX.md` 和 `CHANGELOG.md`。
- 草稿进入正式目录前，必须完成审核并移除未确认内容。

---

## 仓库卫生

- 不提交 `.DS_Store`。
- 不提交 `.obsidian/graph.json`、`.obsidian/workspace.json` 等设备相关的 Obsidian 视图与工作区状态。
- 不使用空目录或 `.DS_Store` 表示知识库结构；目录应至少包含一个有意义的 Markdown 文件。
